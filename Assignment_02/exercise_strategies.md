Exercise 3: 
In order to reverse all the elements in a stack I would create a new stack object and just append them into the new stack as I pop from the original. 

Exercise 4: 
As I iterate through the string I will append a 1, 2, or 3. Then if i see the closing I will peek at the top value in the stack and if my values match up, pop the value. If not, return False. and at the end if I iterate through the whole thing and the stack is empty. Return True or else False. 
I choose to implement this Exercise. 

Exercise 5: 
Id make a new blank q. Then I would pop all the values in the stack into the q. Then I would deq all the values from the q into the stack again by appending. This makes the stack reversed right now. Then I would do the process again into the q. Then deq again into the stack. This time it will be unreversed and be a perfect copy of the original stack. 